export const FAKER_OPTIONS = {
    INT: {
        min: Number.MIN_SAFE_INTEGER,
        max: Number.MAX_SAFE_INTEGER
    },
    FLOAT: {
        min: Number.MIN_SAFE_INTEGER,
        max: Number.MAX_SAFE_INTEGER
    },
    STRING: {
        length: {
            min: 10,
            max: 100
        }
    }
};
